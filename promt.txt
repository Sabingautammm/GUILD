The frontend is CONFIRMED correct now. Do NOT modify the frontend.

Production Vercel Network shows the actual POST request payload as:

{
  "idToken": "[TOKEN]"
}

The request is:

POST https://guild-backend-ow9l.onrender.com/api/auth/google

The CORS preflight succeeds:

OPTIONS /api/auth/google
204 No Content

But the POST still returns:

400 Bad Request

Response:

{
  "message": "Unexpected token '\"', \"\"{\\\"idToke\"... is not valid JSON"
}

This is now a BACKEND debugging task.

IMPORTANT:
Do NOT assume the frontend is double-stringifying anymore. The browser Network tab confirms the request payload is a normal JSON object.

==================================================
GOAL
==================================================

Find EXACTLY why the backend is trying to parse a JSON string containing:

"{\"idToken\":\"...\"}"

even though the browser sends:

{"idToken":"..."}

==================================================
1. INSPECT BACKEND REQUEST PARSING
==================================================

Find the Express server entry point, probably something like:

src/Server.js
src/server.js
app.js
index.js

Inspect middleware order.

Look for:

express.json()
express.urlencoded()
bodyParser.json()
bodyParser.urlencoded()
express.raw()
express.text()
custom body parsing
JSON.parse()

Determine exactly what middleware runs BEFORE:

POST /api/auth/google

==================================================
2. SEARCH THE ENTIRE BACKEND FOR JSON.parse
==================================================

Run a complete search for:

JSON.parse(
JSON.parse(req.body
JSON.parse(req.body.idToken
JSON.parse(request.body
JSON.parse(body

Pay special attention to the Google authentication route.

If the backend already uses:

app.use(express.json())

then req.body should already be a JavaScript object.

The route should NOT manually do:

JSON.parse(req.body)

or:

JSON.parse(JSON.stringify(req.body))

unless there is a very specific reason.

==================================================
3. INSPECT /api/auth/google
==================================================

Find the exact controller/function.

Determine whether it does something like:

const { idToken } = req.body;

This is expected.

Then determine what happens next.

Check for:

JSON.parse(idToken)

JSON.parse(req.body)

JSON.parse(request.body)

JSON.parse(payload)

Any manual JSON parsing around the Google token is suspicious.

==================================================
4. ADD SAFE TEMPORARY DEBUGGING
==================================================

Add temporary logs BEFORE the Google token verification.

DO NOT log the actual token.

Log only:

console.log("Google auth body type:", typeof req.body);
console.log("Google auth body isArray:", Array.isArray(req.body));
console.log("Google auth body keys:", Object.keys(req.body || {}));
console.log("Google auth idToken type:", typeof req.body?.idToken);
console.log("Google auth idToken exists:", Boolean(req.body?.idToken));

Expected output should be approximately:

Google auth body type: object
Google auth body isArray: false
Google auth body keys: [ 'idToken' ]
Google auth idToken type: string
Google auth idToken exists: true

If instead req.body is a string, find WHY it is a string.

==================================================
5. CHECK THE EXACT JSON PARSER ERROR
==================================================

The error:

Unexpected token '"', ""{\"idToke"... is not valid JSON

suggests that somewhere the backend is attempting to parse a value that begins with a quote and contains escaped JSON.

Find the exact line generating this error.

Search for:

try/catch
res.status(400)
error.message
JSON.parse
/api/auth/google

Do not simply catch and hide the error.

==================================================
6. CHECK FOR CUSTOM MIDDLEWARE
==================================================

Search for any middleware that modifies req.body.

Examples:

req.body = JSON.parse(...)
req.body = JSON.stringify(...)
req.body = body
req.body = JSON.parse(JSON.stringify(req.body))

Also check middleware specifically applied to /api/auth/google.

==================================================
7. CHECK GOOGLE AUTH LIBRARY
==================================================

After request parsing is fixed, inspect Google token verification.

Determine whether the backend uses:

google-auth-library
OAuth2Client
verifyIdToken()

The expected flow should be conceptually:

const { idToken } = req.body;

const ticket = await client.verifyIdToken({
  idToken,
  audience: GOOGLE_CLIENT_ID,
});

Do NOT bypass token verification.

Do NOT decode the JWT manually and trust its contents.

==================================================
8. CHECK GOOGLE CLIENT ID
==================================================

The frontend Google Client ID is:

11417688799-3glnd99h17gckc0f5g4imj2jhd4v26in.apps.googleusercontent.com

Check that the backend verifies the token against the correct audience/client ID.

Do NOT print secret environment variables.

==================================================
9. VERY IMPORTANT: DO NOT CHANGE FRONTEND
==================================================

The frontend has already been verified.

Vercel production deployment:

commit:
9f3922a

Production branch:
main

Network Request Payload:

{
  "idToken": "[TOKEN]"
}

Therefore do NOT change:

fetch()
axios()
JSON.stringify()
GoogleLogin
Google callback
frontend authApi

unless backend investigation proves the frontend is somehow making a different request.

==================================================
10. TEST LOCALLY
==================================================

Run the backend locally if possible.

Send a test request with a placeholder:

{
  "idToken": "TEST_TOKEN"
}

The purpose is ONLY to inspect request parsing.

Expected:

typeof req.body === "object"

typeof req.body.idToken === "string"

Then Google verification can reject TEST_TOKEN normally.

The important thing is that JSON parsing itself must succeed.

==================================================
11. CHECK DEPLOYED BACKEND
==================================================

After fixing, commit and push the backend changes to the branch that Render deploys.

Do not assume Render has automatically deployed the latest backend commit.

Verify the Render deployment contains the new commit.

==================================================
12. FINAL REPORT
==================================================

Return:

ROOT CAUSE:
<exact root cause>

FILE:
<exact backend file>

FUNCTION:
<exact function>

BAD CODE:
<exact relevant code>

FIXED CODE:
<exact relevant code>

REQUEST BODY BEFORE ROUTE:
<object/string and why>

WHY THE ERROR OCCURRED:
<clear explanation>

GOOGLE TOKEN VERIFICATION:
<how it works>

DEPLOYMENT:
<backend commit/deployment status>

BUILD/TEST:
<results>

IMPORTANT:
Do not make speculative changes.
Do not modify frontend code.
Do not disable Google token verification.
Do not log or expose the actual Google ID token.
Find the exact backend line responsible for the JSON parsing error.